# 저장소 작업 원칙

이 규칙은 이 저장소에서 반복하는 모든 코드 작업에 적용합니다.

## 문서 로딩 경량화

- 일반 작업은 현재 Issue/요청 + `AGENTS.md` + 직접 수정할 인접 파일/테스트를 확인합니다.
- 큰 기능·여러 모듈 변경은 관련 운영문서와 현재 PR/review를 추가로 확인합니다.
- 아키텍처·렌더링·보안·권한·데이터 저장 구조·배포 구조 변경에서만 중앙 공통 표준 원문을 반드시 다시 확인합니다.
- 중앙 표준은 모든 작업에서 반복 정독하는 체크리스트가 아니라 아키텍처 판단과 감사의 상위 기준입니다.

## 공통 개발 헌법과 프로젝트 규칙

이 저장소의 프로젝트별 규칙은 공통 개발 기준을 대체하지 않고 구체화합니다.

아키텍처·JavaScript 책임 경계·렌더링·동적 데이터·오류 복구·이미지 로딩의 구조적 선택을 변경할 때는 공통 원본 `cetin072/ai-development-system/docs/WEB_ARCHITECTURE_STANDARD_V1.md`를 확인합니다.

핵심 해석:
- `Static by Default, Dynamic by Necessity`를 따르되, 이 앱은 JavaScript 중심의 동적 행사 운영 도구이므로 JS 사용량 자체를 줄이는 것을 목표로 하지 않습니다.
- 핵심 App Shell과 행사 관리·진행 진입점은 결정적으로 존재해야 하며, 선택적 DOM 요소 하나의 실패가 이후 핵심 이벤트 등록 전체를 막아서는 안 됩니다.
- loading / empty / error / retry 상태가 필요한 기능은 구분합니다.
- 기존 행사 데이터와 `localStorage` 구조는 프로젝트의 중요 자산이며, 공통 표준 준수를 이유로 임의 변경하지 않습니다.
- 공통 표준과 프로젝트의 명확한 요구가 충돌하면 프로젝트 요구를 우선할 수 있으나, 예외 이유를 Issue/PR에 기록합니다.

관련 감사 기준은 Issue #13을 따릅니다.

## 작업 규칙

1. `main` 브랜치에서 직접 작업하지 않습니다.
2. 모든 새 작업은 최신 원격 `main`에서 `codex/작업명` 형식의 브랜치를 생성해 진행합니다.
3. 작업 시작 전 기준이 된 원격 `main` 커밋 SHA를 기록합니다.
4. 작업 전에 관련 파일, 기존 기능, 열려 있는 관련 Pull Request를 확인합니다.
5. 파일 전체를 교체하기 전에 기존 최신 코드와 비교합니다.
6. 수정 요청과 관계없는 기능을 이전 버전으로 되돌리지 않습니다.
7. 기존 기능을 임의로 삭제하거나 크게 변경하지 않습니다.
8. 데이터 구조나 저장 방식을 변경해야 한다면 먼저 사용자 승인을 받습니다.
9. 기존 테스트를 삭제하거나 비활성화하지 않습니다.
10. 기존 회귀 테스트를 모두 통과해야 작업 완료로 보고합니다.
11. 기능 수정 후 `npm run test`와 `npm run build`를 실행합니다.
12. 테스트 실패 상태에서는 작업 완료로 보고하지 않습니다.
13. 작업 완료 후 Pull Request를 생성합니다.
14. 사용자가 Netlify Deploy Preview에서 핵심 기능을 직접 확인하기 전에는 `main`에 병합하지 않습니다.
15. 변경 파일, 테스트 결과, 기존 기능과 데이터 영향 여부를 한국어로 설명합니다.
16. API 키, 비밀번호, 개인정보를 코드나 저장소에 추가하지 않습니다.
